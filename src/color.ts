/**
 * @deprecated These literals predate the token system and are not theme aware.
 * Use the generated custom properties instead - `var(--ca-interactive)`,
 * `var(--ca-border)` and `var(--ca-border-subtle)` - or the `token()` helper
 * from `./theme`.
 */

export const primary = '#0076CE';

export const border = '#CCCCCC';
export const borderSecondary = '#E0E0E0';
